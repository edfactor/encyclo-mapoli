{
  "name": "west-monroe",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:api": "cd services/api && .venv/bin/python -m uvicorn api.main:app --reload --port 8000",
    "build:frontend": "npm run build --workspace=frontend"
  }
}
